import NavBar from "../navbar/navbar";
import NavRoute from "../../router/router";

const Layout = () => {
  return (
    <div>
      <NavBar />
      <NavRoute />
    </div>
  );
};

export default Layout;
